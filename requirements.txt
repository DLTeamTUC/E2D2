# E2D2 — Python dependencies
#
# These are the versions used to produce the results reported in the paper.
# Tested with Python 3.8.18 on Linux + CUDA 12.1 (NVIDIA RTX 3060).
#
# The five exact pins below (==) are the versions printed by Cell 0.1 of
# the notebook. The remaining packages are listed with compatible-release
# constraints (~=) since their exact patch versions were not recorded; any
# patch within the same minor series should reproduce the paper results.
#
# Note: Python 3.8 reached end-of-life on 2024-10-14. Newer Python versions
# (3.9 - 3.11) are very likely to work, but require relaxing some of the
# pins below (in particular, scikit-learn >= 1.3 and scipy >= 1.10 dropped
# Python 3.8 support in their following releases).

# --- Core scientific stack ---
numpy==1.24.4
pandas==2.0.3
scipy~=1.10.1
scikit-learn~=1.3.2

# --- Deep learning ---
# Install the CUDA 12.1 build of PyTorch:
#   pip install torch==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# For a CPU-only install, replace with: torch==2.4.1
torch==2.4.1

# --- Tree models and explainability ---
xgboost==2.0.3
shap==0.42.1

# --- Plotting ---
matplotlib~=3.7.5
seaborn~=0.13.2

# --- Notebook ---
jupyter~=1.0.0
ipykernel~=6.29.0

# --- Progress bars (used by SHAP and a few notebook utilities) ---
tqdm~=4.66.0
